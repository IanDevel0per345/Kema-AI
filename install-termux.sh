termux-setup-storage && pkg update -y && pkg upgrade -y && pkg install proot-distro git curl wget jq unzip -y && git clone https://github.com/IanDevel0per345/Kema-AI.git && cd Kema-AI && proot-distro install ubuntu && proot-distro login ubuntu -- bash -c "apt update && apt install curl wget git -y && curl -fsSL https://bun.sh/install | bash && export PATH=\"\$HOME/.bun/bin:\$PATH\" && curl -fsSL https://ollama.com/install.sh | sh && wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-arm64 && chmod +x cloudflared-linux-arm64 && nohup ollama serve > ollama.log 2>&1 & sleep 5 && cd \$(find / -name Kema-AI -type d 2>/dev/null | head -n 1) && bun install && nohup bun run dev:api > api.log 2>&1 & nohup /root/cloudflared-linux-arm64 tunnel --url http://localhost:8008 > /root/tunnel.log 2>&1 & sleep 10 && grep -o 'https://.*\.trycloudflare\.com' /root/tunnel.log"
